import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { StartQuizComponent } from './start-quiz.component';

export const StartQuizRoutes: Routes = [{
  path: '',
  component: StartQuizComponent
}];
